import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  card: {
    width: '100%',
  },
  cardContainer: {
    margin: 5,
  },
  cardBody: {
    borderRadius: 15,
    backgroundColor: '#FFF',
    overflow: 'hidden',
  },
});

export default style;
